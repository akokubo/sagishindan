/*jslint browser, devel */
/*global $, window */

$(document).ready(function () {
    "use strict";

    sessionStorage.removeItem("type");
    sessionStorage.removeItem("fraud_name");
    sessionStorage.removeItem("fraud_image");
    sessionStorage.removeItem("matrix");
});
